env:
  DOCKER_COMPOSE_VERSION: 1.12.0

sudo: required

services:
  - docker
  - mysql

script:
  - cp ./settings.travis-ci.py ./settings.py
  - docker-compose --file docker-compose.travis-ci.yml run -p 127.0.0.1:3306:3306 mltshp ./test.py
